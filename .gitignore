# Dependencies
node_modules/

# OS / editor
.DS_Store
Thumbs.db
*.log

# Local tooling
.claude/
.mcp.json
tools/__pycache__/

# Private spec — contains pricing / contract details, keep out of public repo
ТЗ.md

# Rejected local image experiments — not part of the website
assets/photos/hero-rocks-pile.webp

# Присланные в Telegram исходники логотипов — рабочие файлы, не сайт
assets/photo_*.jpg

# Отчёты кросс-браузерных прогонов и Lighthouse — 12МБ скриншотов и JSON,
# пересоздаются командой node tools/crossbrowser.mjs / tools/lighthouse.mjs
tools/.crossbrowser/

# Разбор ролика героя: 200МБ покадровых PNG и сравнений, из которых видно,
# где расползается вырезка фона. Пересоздаётся из самого ролика:
# ffmpeg -c:v libvpx-vp9 -i assets/video/bottle-loop-alpha.webm -vf format=rgba out/f%03d.png
tools/qa-bottle/

# Выгрузка страницы «История» со старого сайта: дамп HTML, исходные JPEG на 9МБ
# и контактный лист для проверки, что на кадрах. Скрипты рядом остаются в репо,
# всё это пересоздаётся: python tools/history-src/grab.py
tools/history-src/raw/
tools/history-src/page.html
tools/history-src/history.json
tools/history-src/sheet.jpg
